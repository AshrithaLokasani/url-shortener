# Local development defaults (safe to commit — dummy credentials only).
# Override by copying to .env or exporting vars in your shell.
DATABASE_URL=postgres://shortener:shortener@localhost:5432/shortener?sslmode=disable
BASE_URL=http://localhost:8080
HTTP_ADDR=:8080
MIGRATIONS_DIR=migrations
